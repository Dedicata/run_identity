<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <template
        id="web_login_hide_password_form"
        inherit_id="web.login"
        name="Dedicata Run Hide Password Login"
    >
        <xpath expr="//form[@method='post']" position="attributes">
            <attribute
                name="t-att-style"
            >"display:none" if (request.env['dedicata.run.identity'].sudo().should_hide_password_form() and not request.params.get('dedicata_local')) else None</attribute>
        </xpath>
    </template>
</odoo>
